export const breakpoints = {
    xs: 320, // mobile (our minimum supported width)
    sm: 600, // tablet
    md: 900, // desktop and beyond
    lg: 1200,
    xl: 1536
} as const;

export default breakpoints;
